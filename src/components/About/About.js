import {View, Text} from 'react-native';
import React from 'react';

const About = () => {
  return (
    <View
      style={{display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{fontSize: 50, color: 'black'}}>About</Text>
    </View>
  );
};

export default About;
